const input: string = "bg-input-bg border text-black/60 rounded-lg font-mulish p-4 border-input";

const placeholderTextColor: string = "rgba(0, 0, 0, 0.6)";


const classes = {
    input,
    placeholderTextColor
};

export default classes;
